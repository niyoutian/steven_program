sudo apt-get install autoconf
sudo apt-get install libmysqlclient-dev



autoscan
autoheader
aclocal
autoconf
automake --add-missing
./configure
make

autoscan;autoheader;aclocal;autoconf;automake --add-missing;./configure;make
